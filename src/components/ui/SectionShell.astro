---
interface Props {
  class?: string;
}

const className = ['section-shell', Astro.props.class].filter(Boolean).join(' ');
---

<section class={className}>
  <div class="container">
    <slot />
  </div>
</section>
